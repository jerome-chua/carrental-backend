export default function initBookingsController(db) {
  const getBookings = async (req, res) => {
    try {
      console.log('testg');
    } catch (err) {
      console.log(err);
    }
  };

  return {
    getBookings,
  };
}
